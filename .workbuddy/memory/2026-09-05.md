# 2026-09-05

## 本地环境启动（能碳管理平台原型）
- 项目根目录缺 `node_modules`，首次启动需 `npm install`（101 包，约 1 分钟）。
- 必须用隔离 node：`export PATH="/Users/chengfei/.workbuddy/binaries/node/versions/22.22.2-2/bin:$PATH"`（node v22.22.2 / npm 10.9.7）。
- 技术栈：Vite 8.2.2 + React 19 + TS 6 + antd 6 + ECharts 6 + react-router 7。
- Vite 端口固定 8088 且 `strictPort: true`，端口被占会直接启动失败；`host: true` 默认已暴露局域网。
- 启动后：Local http://localhost:8088/ ，Network http://192.168.1.199:8088/。
- 冒烟：index 与 /src/main.tsx 均 200；`npx tsc --noEmit` 零错误。
- 注意：README 里写的启动目录是 `frontend_demo`，与当前实际目录名不一致，属文档遗留错误。

## 浏览器实测主链路（agent-browser + Chromium）
- 已验证通过的闭环：
  1. 驾驶舱下钻：园区总览企业名 → /archive/enterprises?id=ent-001 → 自动弹出企业档案抽屉 ✅
  2. 告警转工单：/ops/monitor 故障行「查看告警」→ /ops/alarms「转工单」→ 自动跳 /ops/workorders，toast 返回 wo-demo-* 工单号 → 开始处理（已派工→处理中）→ 完成（已完成）✅
  3. 碳核算任务流：/carbon/accounting 新建任务（弹窗预填默认值）→ 执行核算两次（草稿→核算中→审核中）→ 审核通过（已发布，toast 确认）；状态机校验正常——审核中再点执行核算会被 toast「当前状态『审核中』不可执行核算」正确拦截 ✅
  4. 能源一张图：园区示意图 + 企业点位正常渲染 ✅
  5. 全页截图确认：KPI 卡、ECharts 柱状/环形图、表格 CRUD 均正常 ✅
- 结论：原型在本地可正常演示，四大核心闭环全部通过，无功能 bug。
- 自动化测试坑：agent-browser 原生 click 对表格内 React Router Link 无效（报 Done 但 URL 不变，疑坐标遮挡），改用 `eval` 执行 DOM `.click()` 或按按钮文本 JS 点击可绕过；antd 弹窗按钮带空格（"创 建"），匹配时需 strip 空白。
- 截图存于 /tmp/nengtan-*.png（11 张，含首页/抽屉/工单/核算/地图）。

## 生产构建（npm run build）
- 构建成功：13 秒，tsc 零错误，3781 模块，产物 `dist/`（index.html + 单 JS 2.83MB / gzip 874KB + CSS 0.32KB）。
- 构建警告：chunk >500KB（单包无代码分割）。未处理——原型场景可接受，若要优化需 antd/echarts 按需或 manualChunks。
- vite preview 已验证：root/js/deep-route(/carbon/accounting) 均 200（vite preview 自带 SPA fallback）。
- 浏览器实测生产包：首页渲染正常、告警转工单链路正常（无白屏、无运行时错误）。
- ⚠️ 部署风险：`index.html` 资源引用是绝对路径 `/assets/...`（vite base='/'），只能部署在域名根路径；放子目录（如 /nengtan/）会 404，需改 vite.config.ts 加 `base: './'` 或对应子路径。

## 部署风险实证（纯静态服务器 + 子目录模拟）
- 长期保留服务：dev 8088（task bcjDpQ）+ preview 4173（task imZrAV），均在后台运行。
- 纯静态服务器（python http.server，无 fallback）：入口页 200，客户端导航正常（实测导航到 /carbon/accounting 成功），**刷新/直达深路由 404**（curl 证实）。
- 子目录部署（/nengtan/，base 未改）：**完全白屏**——JS 引用 `/assets/...` 解析到域名根 404，body 0 字符（浏览器实测）。
- **base:'./' 是伪解**：BrowserRouter 深路由刷新时相对路径解析成 /carbon/assets/... 必炸；子目录部署必须写死 `base: '/nengtan/'`。
- 修复：index.html favicon 脚手架残留 `/vite.svg`（404）→ 改 `/favicon.svg`，已重新构建验证 200。
- 交付：`DEPLOY.md`（实证矩阵 + nginx try_files 配置 + 各托管平台做法），置于项目根目录。
- 测试服务器 8098/8099 已关，/tmp/subdir-test 已清理。长期保留：dev 8088 + preview 4173。
